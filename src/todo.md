# TODO: create ukjp element templating system that allows for properties and innerHTML etc. and the build.ts file with a CLI.

## Where i am : just started, need decorators for @UIComponent({settings})
